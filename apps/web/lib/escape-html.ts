// Re-export of the shared HTML-escaping Module (packages/utils/src/html.ts) —
// kept so existing `@/lib/escape-html` importers don't churn.
export { escapeHtml, headerSafe } from "@askarthur/utils/html";
